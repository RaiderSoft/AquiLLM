# Core Settings
SECRET_KEY=django-insecure-change-this-in-production
DJANGO_DEBUG=1
PORT=8080
HOST_NAME=localhost

# Database Configuration (these values match docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=jacksonmadden
POSTGRES_NAME=postgres
POSTGRES_HOST=db

# Storage Configuration (these values match docker-compose.yml for MinIO)
STORAGE_HOST=storage:9000
STORAGE_ACCESS_KEY=dev
STORAGE_SECRET_KEY=rickbailey

# Authentication (ONLY required for production mode when DJANGO_DEBUG=0)
# For development, you can leave these as placeholders and create a superuser instead
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH2_CLIENT_SECRET=your-google-client-secret
ALLOWED_EMAIL_DOMAINS=example.edu,example.org
ALLOWED_EMAIL_ADDRESSES=your@email.com,another@email.com
WM_EMAIL=admin@example.com

# LLM Configuration - At least one API key is REQUIRED
LLM_CHOICE=CLAUDE
# Add your API keys below (at least one is required):
ANTHROPIC_API_KEY=your-claude-api-key
OPENAI_API_KEY=your-openai-api-key
GEMINI_API_KEY=your-gemini-api-key
COHERE_KEY=your-cohere-api-key